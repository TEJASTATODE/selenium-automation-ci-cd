image: node:18

stages:
  - test

selenium_tests:
  stage: test
  before_script:
    - apt-get update
    - apt-get install -y chromium chromium-driver
    - which chromium
    - which chromedriver
    - chromium --version
    - chromedriver --version
    - npm install
  script:
    - npm test
